#Update Android SDK through command line

1. To get the list of the updated plugins

`android list sdk --all`

2. To update one plugin:

`android update sdk --no-ui --all --filter <number in the list>`
